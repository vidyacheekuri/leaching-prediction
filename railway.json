{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import os; import subprocess; port = os.environ.get('PORT', '8080'); subprocess.run(['gunicorn', 'app:app', '--bind', f'0.0.0.0:{port}', '--workers', '2', '--threads', '2', '--timeout', '120'])\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

